% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quran_config.R
\name{quran_config}
\alias{quran_config}
\title{Create a Quran Analysis Configuration Object}
\usage{
quran_config(
  by = "surah",
  ngram = "unigram",
  top_n = 20L,
  remove_stopwords = TRUE,
  stopword_lang = "en",
  normalize = TRUE,
  remove_special = TRUE,
  remove_words = NULL
)
}
\arguments{
\item{by}{Character. The dimension to group analysis by. One of:
\code{"surah"} (default), \code{"juz"}, or \code{"ayah"}.}

\item{ngram}{Character. The type of token to use. One of:
\code{"unigram"} (default), \code{"bigram"}, or \code{"trigram"}.}

\item{top_n}{Integer. Number of top terms to return by frequency. Default \code{20}.}

\item{remove_stopwords}{Logical. Whether to remove stop words. Default \code{TRUE}.}

\item{stopword_lang}{Character. Language code for stop word removal (ISO 639-1
codes preferred, e.g. \code{"en"} for English, \code{"ms"} for Malay,
\code{"id"} for Indonesian). Full language names (e.g. \code{"english"})
are also accepted if supported by the \code{snowball} source.
Default \code{"en"}.}

\item{normalize}{Logical. Whether to apply word stemming via SnowballC. 
Default \code{TRUE}. Automatically skipped for languages not supported by
SnowballC (e.g. Malay \code{"ms"}), with a message to the user.}

\item{remove_special}{Logical. Whether to strip punctuation and numbers.
Default \code{TRUE}.}

\item{remove_words}{Character vector. Additional custom words to remove.
Default \code{NULL}.}
}
\value{
An S3 object of class \code{QuranConfig}.
}
\description{
Creates a \code{QuranConfig} S3 object that bundles all text preprocessing
and analysis parameters. This config object is passed to analytic functions
such as \code{tf_trans()} and \code{wordcloud_trans()}, ensuring a consistent
and reusable input pattern across all functions in the XploreQuran package.
}
\examples{
# Default English config
cfg <- quran_config()

# Malay config with bigrams and no normalization
cfg_ms <- quran_config(
  by               = "juz",
  ngram            = "bigram",
  top_n            = 10,
  stopword_lang    = "ms",
  remove_stopwords = TRUE,
  normalize        = FALSE,
  remove_words     = c("allah", "said")
)
}
